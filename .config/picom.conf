backend = "glx"
vsync = false
shadow = false

animations = (
  {
    triggers = [ "open" ];
    preset = "appear";
    duration = 0.1;
  },
  {
    triggers = [ "close" ];
    preset = "disappear";
    duration = 0.1;
  },
  {
    triggers = [ "geometry" ];
	preset = "geometry-change";
    duration = 0.1;
  }
)
